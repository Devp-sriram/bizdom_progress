{/* {cart.map(item => {
                    return (
                        <div className="d-flex justify-content-between">
                            <p>
                                {reduceLength(item.title, 20)}
                            </p>
                            <p>
                                x {item.qty || 1}
                            </p>
                            <p>
                                ₹ {(item.price + item.price * 5 / 100).toFixed(2) * (item.qty || 1)}
                            </p>
                        </div>
                    )
                })} */}